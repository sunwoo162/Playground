[package]
name = "bloom-runtime"
version = "0.1.0"
description = "Headless agent runtime for Bloom"
authors = ["sunwoo162"]
edition = "2021"

[lib]
name = "bloom_runtime"
crate-type = ["rlib"]

[dependencies]
tauri = { version = "2", features = [] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
