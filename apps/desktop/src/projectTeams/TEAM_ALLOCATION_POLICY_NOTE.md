# Team Allocation Policy Note

The default allocator is intentionally neutral. It balances opportunities across idle teams using assignment history only. It does not rank teams by perceived quality or assign specialties without measured evidence.

This note exists to keep future changes from silently turning the equal-status five-team model into a hierarchy. Any future capability-based routing should require explicit, auditable metrics and a separate policy change.
